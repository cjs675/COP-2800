import javax.swing.*;

public class UnitConversion
{
    public static void main(String[] args)
    {
        int selection;
        String selectionOther;
     /*   selection = JOptionPane.showConfirmDialog(null,
                "Select the base unit you would like to convert from: ");
        isYes = (selection == JOptionPane.YES_NO_OPTION); */
        JOptionPane.showMessageDialog(null,
                "You responded " + isYes);
        selectionOther = JOptionPane.showInternalInputDialog(null,
                "Inches");
    }
}